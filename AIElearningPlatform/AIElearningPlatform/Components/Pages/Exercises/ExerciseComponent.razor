@page "/exercise-page"

<PageTitle>Exercise Page</PageTitle>

<div class="container-fluid min-vh-100 d-flex flex-column justify-content-center align-items-center" style="background-color: #13131a; color: white; padding: 50px;">
    <!-- Header -->
    <h1 class="text-center mb-4" style="color: white; font-weight: bold;">eLearningSchool Quizzes</h1>
    <p class="text-center mb-5" style="color: rgba(255, 255, 255, 0.8); font-size: 1.2rem;">Test your skills with eLearningSchool's Quizzes.</p>

    <!-- Quiz Section -->
    <h3 class="text-center mt-5" style="color: white; font-weight: bold;">The Quiz</h3>
    <p class="text-center mt-2" style="color: rgba(255, 255, 255, 0.8);">Each quiz contains 25-40 questions. You get 1 point for each correct answer. At the end of each quiz, you get your total score.</p>
    <p class="text-center" style="color: rgba(255, 255, 255, 0.8);">When you finish the quiz, you can go through each question with the correct answer.</p>

    <!-- Exercises Header -->
    <h1 class="text-center mt-5 mb-4" style="color: white; font-weight: bold;">eLearningSchool Exercises</h1>
    <p class="text-center mb-5" style="color: rgba(255, 255, 255, 0.8); font-size: 1.2rem;">Test your skills with eLearningSchool's Exercises.</p>

    <!-- Exercises Section -->
    <h3 class="text-center mt-5" style="color: white; font-weight: bold;">Exercise</h3>
    <p class="text-center mt-2" style="color: rgba(255, 255, 255, 0.8);">We have gathered a variety of exercises (with answers) for each tutorial.</p>
    <p class="text-center" style="color: rgba(255, 255, 255, 0.8);">Try to solve an exercise by editing some code. Get a "hint" if you're stuck, or show the answer to see what you've done wrong.</p>

    <!-- Language Cards -->
    <div class="row mt-5">
        @foreach (var course in Courses)
        {
            <div class="col-md-4 mb-4">
                <div class="card text-center shadow-lg" style="background-color: #2c2c39; border-radius: 70px; padding: 20px;">
                    <h3 style="color: white; font-weight: bold;">@course.Name</h3>
                    <p style="color: rgba(255, 255, 255, 0.8);">@course.QuestionsCount Questions</p>
                    <div class="d-flex justify-content-center">
                        <button class="btn" style="background-color: #4e4e7e; color: white; border: none; padding: 5px 20px; border-radius: 20px; font-size: 0.9rem;">Start Quiz</button>
                    </div>
                </div>
            </div>
        }
    </div>

</div>

@code {
    private List<Course> Courses = new()
    {
        new Course { Name = "HTML", QuestionsCount = 10 },
        new Course { Name = "CSS", QuestionsCount = 8 },
        new Course { Name = "JavaScript", QuestionsCount = 12 },
        new Course { Name = "SQL", QuestionsCount = 12 },
        new Course { Name = "MySQL", QuestionsCount = 12 },
        new Course { Name = "Python", QuestionsCount = 12 },
        new Course { Name = "PHP", QuestionsCount = 12 },
        new Course { Name = "Bootstrap", QuestionsCount = 12 },
        new Course { Name = "jQuery", QuestionsCount = 12 },
        new Course { Name = "React", QuestionsCount = 12 },
        new Course { Name = "Java", QuestionsCount = 12 },
        new Course { Name = "C", QuestionsCount = 12 },
        new Course { Name = "C++", QuestionsCount = 12 },
        new Course { Name = "C#", QuestionsCount = 12 },
        new Course { Name = "Kotlin", QuestionsCount = 12 },
        new Course { Name = "TypeScript", QuestionsCount = 12 },
        new Course { Name = "MongoDB", QuestionsCount = 12 },
    };

    private class Course
    {
        public string Name { get; set; } = "";
        public int QuestionsCount { get; set; } = 0;
    }
}
